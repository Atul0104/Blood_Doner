# Code Citations

## License: unknown
https://github.com/camara94/monsiteperso2/blob/08166fcf45e91be3f23d4f291525bbaa47aca89b/src/app/contact/contact.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      icon
```


## License: unknown
https://github.com/iclavijos/msdb/blob/414a79d4673e093eabbe4a053a8618b570252b14/src/main/webapp/app/entities/racetrack/list/racetrack.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -
```


## License: unknown
https://github.com/camara94/monsiteperso2/blob/08166fcf45e91be3f23d4f291525bbaa47aca89b/src/app/contact/contact.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      icon
```


## License: unknown
https://github.com/iclavijos/msdb/blob/414a79d4673e093eabbe4a053a8618b570252b14/src/main/webapp/app/entities/racetrack/list/racetrack.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -
```


## License: unknown
https://github.com/camara94/monsiteperso2/blob/08166fcf45e91be3f23d4f291525bbaa47aca89b/src/app/contact/contact.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      icon
```


## License: unknown
https://github.com/iclavijos/msdb/blob/414a79d4673e093eabbe4a053a8618b570252b14/src/main/webapp/app/entities/racetrack/list/racetrack.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -
```


## License: unknown
https://github.com/camara94/monsiteperso2/blob/08166fcf45e91be3f23d4f291525bbaa47aca89b/src/app/contact/contact.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      icon
```


## License: unknown
https://github.com/iclavijos/msdb/blob/414a79d4673e093eabbe4a053a8618b570252b14/src/main/webapp/app/entities/racetrack/list/racetrack.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -
```


## License: unknown
https://github.com/camara94/monsiteperso2/blob/08166fcf45e91be3f23d4f291525bbaa47aca89b/src/app/contact/contact.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      icon
```


## License: unknown
https://github.com/iclavijos/msdb/blob/414a79d4673e093eabbe4a053a8618b570252b14/src/main/webapp/app/entities/racetrack/list/racetrack.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -
```


## License: unknown
https://github.com/iclavijos/msdb/blob/414a79d4673e093eabbe4a053a8618b570252b14/src/main/webapp/app/entities/racetrack/list/racetrack.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -
```


## License: unknown
https://github.com/camara94/monsiteperso2/blob/08166fcf45e91be3f23d4f291525bbaa47aca89b/src/app/contact/contact.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -
```


## License: unknown
https://github.com/iclavijos/msdb/blob/414a79d4673e093eabbe4a053a8618b570252b14/src/main/webapp/app/entities/racetrack/list/racetrack.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -
```


## License: unknown
https://github.com/camara94/monsiteperso2/blob/08166fcf45e91be3f23d4f291525bbaa47aca89b/src/app/contact/contact.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -
```


## License: unknown
https://github.com/iclavijos/msdb/blob/414a79d4673e093eabbe4a053a8618b570252b14/src/main/webapp/app/entities/racetrack/list/racetrack.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -
```


## License: unknown
https://github.com/camara94/monsiteperso2/blob/08166fcf45e91be3f23d4f291525bbaa47aca89b/src/app/contact/contact.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -
```


## License: unknown
https://github.com/iclavijos/msdb/blob/414a79d4673e093eabbe4a053a8618b570252b14/src/main/webapp/app/entities/racetrack/list/racetrack.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L
```


## License: unknown
https://github.com/camara94/monsiteperso2/blob/08166fcf45e91be3f23d4f291525bbaa47aca89b/src/app/contact/contact.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L
```


## License: unknown
https://github.com/iclavijos/msdb/blob/414a79d4673e093eabbe4a053a8618b570252b14/src/main/webapp/app/entities/racetrack/list/racetrack.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L
```


## License: unknown
https://github.com/camara94/monsiteperso2/blob/08166fcf45e91be3f23d4f291525bbaa47aca89b/src/app/contact/contact.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L
```


## License: unknown
https://github.com/iclavijos/msdb/blob/414a79d4673e093eabbe4a053a8618b570252b14/src/main/webapp/app/entities/racetrack/list/racetrack.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(
```


## License: unknown
https://github.com/camara94/monsiteperso2/blob/08166fcf45e91be3f23d4f291525bbaa47aca89b/src/app/contact/contact.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(
```


## License: unknown
https://github.com/iclavijos/msdb/blob/414a79d4673e093eabbe4a053a8618b570252b14/src/main/webapp/app/entities/racetrack/list/racetrack.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(
```


## License: unknown
https://github.com/camara94/monsiteperso2/blob/08166fcf45e91be3f23d4f291525bbaa47aca89b/src/app/contact/contact.component.ts

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(
```


## License: unknown
https://github.com/Snack-X/japan-railroad-plotter/blob/88fd946e4ebd9657776614a89005277af48b46f7/src/js/common/utils.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) *
```


## License: unknown
https://github.com/StenOverdijk/GildeAPI/blob/54148c3ea356eb4cbd71b9dd341d92e5a1bc37e9/scripts.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) *
```


## License: unknown
https://github.com/Snack-X/japan-railroad-plotter/blob/88fd946e4ebd9657776614a89005277af48b46f7/src/js/common/utils.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) *
```


## License: unknown
https://github.com/StenOverdijk/GildeAPI/blob/54148c3ea356eb4cbd71b9dd341d92e5a1bc37e9/scripts.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) *
```


## License: unknown
https://github.com/ig2gi/ig2gi.github.com/blob/0b029b22f9c84d00d7500443a39bcc69af74511c/projects/travelmap/sprint1/js/tm-data.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c =
```


## License: unknown
https://github.com/Snack-X/japan-railroad-plotter/blob/88fd946e4ebd9657776614a89005277af48b46f7/src/js/common/utils.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) *
```


## License: unknown
https://github.com/StenOverdijk/GildeAPI/blob/54148c3ea356eb4cbd71b9dd341d92e5a1bc37e9/scripts.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) *
```


## License: unknown
https://github.com/ig2gi/ig2gi.github.com/blob/0b029b22f9c84d00d7500443a39bcc69af74511c/projects/travelmap/sprint1/js/tm-data.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c =
```


## License: unknown
https://github.com/brianpattison/ember-titanium-utilities/blob/9b94fee7000a9894434c92c86befe15016484ba7/geolocation_info.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/jhoscar1/capstone/blob/acd52d7865fb70f7b7ed0f1e22a975e7750b338c/PublicArt/utils/index.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: MIT
https://github.com/vsimonovski/sportify/blob/86d040962a9ba9b45a6951defbe6035993b04534/src/components/Find/Find.vue

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/Snack-X/japan-railroad-plotter/blob/88fd946e4ebd9657776614a89005277af48b46f7/src/js/common/utils.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) *
```


## License: unknown
https://github.com/StenOverdijk/GildeAPI/blob/54148c3ea356eb4cbd71b9dd341d92e5a1bc37e9/scripts.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) *
```


## License: unknown
https://github.com/ig2gi/ig2gi.github.com/blob/0b029b22f9c84d00d7500443a39bcc69af74511c/projects/travelmap/sprint1/js/tm-data.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c =
```


## License: unknown
https://github.com/brianpattison/ember-titanium-utilities/blob/9b94fee7000a9894434c92c86befe15016484ba7/geolocation_info.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/jhoscar1/capstone/blob/acd52d7865fb70f7b7ed0f1e22a975e7750b338c/PublicArt/utils/index.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: MIT
https://github.com/vsimonovski/sportify/blob/86d040962a9ba9b45a6951defbe6035993b04534/src/components/Find/Find.vue

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/Snack-X/japan-railroad-plotter/blob/88fd946e4ebd9657776614a89005277af48b46f7/src/js/common/utils.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) *
```


## License: unknown
https://github.com/StenOverdijk/GildeAPI/blob/54148c3ea356eb4cbd71b9dd341d92e5a1bc37e9/scripts.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) *
```


## License: unknown
https://github.com/ig2gi/ig2gi.github.com/blob/0b029b22f9c84d00d7500443a39bcc69af74511c/projects/travelmap/sprint1/js/tm-data.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c =
```


## License: unknown
https://github.com/brianpattison/ember-titanium-utilities/blob/9b94fee7000a9894434c92c86befe15016484ba7/geolocation_info.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/jhoscar1/capstone/blob/acd52d7865fb70f7b7ed0f1e22a975e7750b338c/PublicArt/utils/index.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: MIT
https://github.com/vsimonovski/sportify/blob/86d040962a9ba9b45a6951defbe6035993b04534/src/components/Find/Find.vue

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/ig2gi/ig2gi.github.com/blob/0b029b22f9c84d00d7500443a39bcc69af74511c/projects/travelmap/sprint1/js/tm-data.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c =
```


## License: unknown
https://github.com/Snack-X/japan-railroad-plotter/blob/88fd946e4ebd9657776614a89005277af48b46f7/src/js/common/utils.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c =
```


## License: unknown
https://github.com/StenOverdijk/GildeAPI/blob/54148c3ea356eb4cbd71b9dd341d92e5a1bc37e9/scripts.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c =
```


## License: unknown
https://github.com/brianpattison/ember-titanium-utilities/blob/9b94fee7000a9894434c92c86befe15016484ba7/geolocation_info.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/jhoscar1/capstone/blob/acd52d7865fb70f7b7ed0f1e22a975e7750b338c/PublicArt/utils/index.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: MIT
https://github.com/vsimonovski/sportify/blob/86d040962a9ba9b45a6951defbe6035993b04534/src/components/Find/Find.vue

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/brianpattison/ember-titanium-utilities/blob/9b94fee7000a9894434c92c86befe15016484ba7/geolocation_info.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/ig2gi/ig2gi.github.com/blob/0b029b22f9c84d00d7500443a39bcc69af74511c/projects/travelmap/sprint1/js/tm-data.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/jhoscar1/capstone/blob/acd52d7865fb70f7b7ed0f1e22a975e7750b338c/PublicArt/utils/index.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: MIT
https://github.com/vsimonovski/sportify/blob/86d040962a9ba9b45a6951defbe6035993b04534/src/components/Find/Find.vue

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/Snack-X/japan-railroad-plotter/blob/88fd946e4ebd9657776614a89005277af48b46f7/src/js/common/utils.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/StenOverdijk/GildeAPI/blob/54148c3ea356eb4cbd71b9dd341d92e5a1bc37e9/scripts.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/brianpattison/ember-titanium-utilities/blob/9b94fee7000a9894434c92c86befe15016484ba7/geolocation_info.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/ig2gi/ig2gi.github.com/blob/0b029b22f9c84d00d7500443a39bcc69af74511c/projects/travelmap/sprint1/js/tm-data.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/jhoscar1/capstone/blob/acd52d7865fb70f7b7ed0f1e22a975e7750b338c/PublicArt/utils/index.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: MIT
https://github.com/vsimonovski/sportify/blob/86d040962a9ba9b45a6951defbe6035993b04534/src/components/Find/Find.vue

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/Snack-X/japan-railroad-plotter/blob/88fd946e4ebd9657776614a89005277af48b46f7/src/js/common/utils.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/StenOverdijk/GildeAPI/blob/54148c3ea356eb4cbd71b9dd341d92e5a1bc37e9/scripts.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a
```


## License: unknown
https://github.com/brianpattison/ember-titanium-utilities/blob/9b94fee7000a9894434c92c86befe15016484ba7/geolocation_info.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
```


## License: unknown
https://github.com/ig2gi/ig2gi.github.com/blob/0b029b22f9c84d00d7500443a39bcc69af74511c/projects/travelmap/sprint1/js/tm-data.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
```


## License: unknown
https://github.com/jhoscar1/capstone/blob/acd52d7865fb70f7b7ed0f1e22a975e7750b338c/PublicArt/utils/index.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
```


## License: MIT
https://github.com/vsimonovski/sportify/blob/86d040962a9ba9b45a6951defbe6035993b04534/src/components/Find/Find.vue

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
```


## License: unknown
https://github.com/Snack-X/japan-railroad-plotter/blob/88fd946e4ebd9657776614a89005277af48b46f7/src/js/common/utils.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
```


## License: unknown
https://github.com/StenOverdijk/GildeAPI/blob/54148c3ea356eb4cbd71b9dd341d92e5a1bc37e9/scripts.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
```


## License: unknown
https://github.com/brianpattison/ember-titanium-utilities/blob/9b94fee7000a9894434c92c86befe15016484ba7/geolocation_info.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
  }
}
````

---

## 3. **Create Navigation Component
```


## License: unknown
https://github.com/ig2gi/ig2gi.github.com/blob/0b029b22f9c84d00d7500443a39bcc69af74511c/projects/travelmap/sprint1/js/tm-data.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
  }
}
````

---

## 3. **Create Navigation Component
```


## License: unknown
https://github.com/jhoscar1/capstone/blob/acd52d7865fb70f7b7ed0f1e22a975e7750b338c/PublicArt/utils/index.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
  }
}
````

---

## 3. **Create Navigation Component
```


## License: MIT
https://github.com/vsimonovski/sportify/blob/86d040962a9ba9b45a6951defbe6035993b04534/src/components/Find/Find.vue

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
  }
}
````

---

## 3. **Create Navigation Component
```


## License: unknown
https://github.com/Snack-X/japan-railroad-plotter/blob/88fd946e4ebd9657776614a89005277af48b46f7/src/js/common/utils.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
  }
}
````

---

## 3. **Create Navigation Component
```


## License: unknown
https://github.com/StenOverdijk/GildeAPI/blob/54148c3ea356eb4cbd71b9dd341d92e5a1bc37e9/scripts.js

```
I'll add complete map navigation functionality to your Angular app. This includes setting up Leaflet maps, creating a navigation service, and integrating it with your components.

## 1. **Install Leaflet Dependencies**

Run these commands in your terminal:

```bash
npm install leaflet
npm install @types/leaflet --save-dev
npm install leaflet-routing-machine
npm install leaflet-control-geocoder
```

---

## 2. **Create Map Service**

````typescript
// filepath: c:\Users\Nikita\Desktop\angular\demo\src\app\services\map.service.ts
import { Injectable } from '@angular/core';
import * as L from 'leaflet';

@Injectable({
  providedIn: 'root'
})
export class MapService {
  private map: L.Map | null = null;
  private markers: L.Marker[] = [];
  private routingControl: any = null;

  constructor() {
    this.initializeLeafletIcons();
  }

  initializeLeafletIcons(): void {
    const iconRetinaUrl = 'assets/marker-icon-2x.png';
    const iconUrl = 'assets/marker-icon.png';
    const shadowUrl = 'assets/marker-shadow.png';
    
    const iconDefault = L.icon({
      iconRetinaUrl,
      iconUrl,
      shadowUrl,
      iconSize: [25, 41],
      iconAnchor: [12, 41],
      popupAnchor: [1, -34],
      tooltipAnchor: [16, -28],
      shadowSize: [41, 41]
    });

    L.Marker.prototype.setIcon(iconDefault);
  }

  initializeMap(elementId: string, center: [number, number] = [19.0760, 72.8777], zoom: number = 13): L.Map {
    if (this.map) {
      return this.map;
    }

    this.map = L.map(elementId).setView(center, zoom);

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(this.map);

    return this.map;
  }

  addMarker(lat: number, lng: number, title: string, popupContent?: string): L.Marker {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    const marker = L.marker([lat, lng])
      .addTo(this.map)
      .bindPopup(popupContent || title)
      .bindTooltip(title);

    this.markers.push(marker);
    return marker;
  }

  addDonorMarkers(donors: any[]): void {
    if (!this.map) {
      throw new Error('Map not initialized');
    }

    this.clearMarkers();

    donors.forEach(donor => {
      const popupContent = `
        <div style="font-family: Arial; width: 200px;">
          <h4>${donor.name}</h4>
          <p><strong>Blood Group:</strong> ${donor.bloodGroup}</p>
          <p><strong>City:</strong> ${donor.city}</p>
          <p><strong>Phone:</strong> ${donor.phone}</p>
        </div>
      `;
      this.addMarker(donor.latitude || 19.0760, donor.longitude || 72.8777, donor.name, popupContent);
    });
  }

  addMultipleMarkers(locations: { lat: number; lng: number; title: string; content?: string }[]): void {
    locations.forEach(location => {
      this.addMarker(location.lat, location.lng, location.title, location.content);
    });
  }

  clearMarkers(): void {
    this.markers.forEach(marker => {
      if (this.map) {
        this.map.removeLayer(marker);
      }
    });
    this.markers = [];
  }

  setCenter(lat: number, lng: number, zoom: number = 13): void {
    if (this.map) {
      this.map.setView([lat, lng], zoom);
    }
  }

  fitBounds(bounds: L.LatLngBounds): void {
    if (this.map) {
      this.map.fitBounds(bounds);
    }
  }

  getMap(): L.Map | null {
    return this.map;
  }

  destroyMap(): void {
    if (this.map) {
      this.map.remove();
      this.map = null;
      this.markers = [];
    }
  }

  getCurrentLocation(): Promise<[number, number]> {
    return new Promise((resolve, reject) => {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            resolve([lat, lng]);
          },
          (error) => {
            console.error('Geolocation error:', error);
            reject(error);
          }
        );
      } else {
        reject(new Error('Geolocation not supported'));
      }
    });
  }

  calculateDistance(lat1: number, lng1: number, lat2: number, lng2: number): number {
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLng = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
      Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLng / 2) * Math.sin(dLng / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
  }
}
````

---

## 3. **Create Navigation Component
```

